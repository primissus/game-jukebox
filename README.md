# Examen (game-jukebox)
## Iniciar el server

Para correr el programa es necesario ejecutar los comandos `npm install` para la instalación de los módulos y posteriormente ejecutart `npm start` para iniciar el servidor.

Es necesario que haya una instancia de mongodb correndo, en caso de no estar iniciada ejecutar `mongod`